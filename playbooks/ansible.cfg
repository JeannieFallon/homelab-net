[defaults]
# Remove if project expands to multiple hosts files
inventory = ./inventory/hosts
# Silence the interpreter discovery warning
interpreter_python = auto_silent
# Use for non-interactive lab runs to ensure Ansible won't hang on new fingerprints
host_key_checking = False